import React from 'react';

import {
  Button,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const Active = ({navigation}) => {
  return (
    <View>
      <Text>This is Active screen</Text>
    </View>
  );
};

export default Active;
